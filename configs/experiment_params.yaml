experiment:
  name: model_selection_burnout_prediction
  tracking:
    # Start a server with: mlflow server --host 127.0.0.1 --port 5000
    tracking_uri: https://dagshub.com/grvgulia007/burnout_classifier.mlflow
    experiment_name: model_selection_burnout_prediction
    selection_metric: macro_f1
    cv_folds: 3
    random_state: 42
    log_model: true
    log_feature_importance: true

models:

  random_forest:
    enabled: true
    search_type: grid

    parameters:
      n_estimators:
        - 100
        - 200
        - 300

      max_depth:
        - 10
        - 20
        - null

      min_samples_split:
        - 2
        - 5

      min_samples_leaf:
        - 1
        - 2

      max_features:
        - sqrt
        - log2

      criterion:
        - gini
        - entropy

      class_weight:
        - balanced

      random_state:
        - 42

      n_jobs:
        - 4


  xgboost:
    enabled: true
    search_type: grid

    parameters:
      n_estimators:
        - 100
        - 200
        - 300

      learning_rate:
        - 0.01
        - 0.05
        - 0.1

      max_depth:
        - 3
        - 5
        - 7

      subsample:
        - 0.8
        - 1.0

      colsample_bytree:
        - 0.8
        - 1.0

      gamma:
        - 0
        - 0.1

      reg_alpha:
        - 0
        - 0.1

      reg_lambda:
        - 1
        - 5

      objective:
        - multi:softprob

      eval_metric:
        - mlogloss

      random_state:
        - 42


  logistic_regression:
    enabled: true
    search_type: grid

    parameters:
      C:
        - 0.01
        - 0.1
        - 1
        - 10

      penalty:
        - l2

      solver:
        - lbfgs
        - saga

      max_iter:
        - 1000

      class_weight:
        - balanced

      random_state:
        - 42
